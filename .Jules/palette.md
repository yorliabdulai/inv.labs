## 2024-03-29 - Login Form Accessible Labels
**Learning:** Missed `htmlFor` and `id` bindings on login forms limit accessibility, and simply adding them along with `cursor-pointer` on labels improves both screen reader behavior and mouse UX instantly without changing the visual layout.
**Action:** Always check form elements for these attributes and ensure proper binding with visual feedback cues.
